{
  // ======================================================================
  // Monorepo Root TypeScript Configuration
  // Configuration for root-level files like knip.config.ts
  // Individual apps/packages have their own specific configurations
  // ======================================================================
  "extends": "./packages/config/typescript-config/base.json",
  "include": ["*.ts", "*.js"],
  "exclude": ["node_modules", "apps", "packages"]
}
